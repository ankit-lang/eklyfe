{% comment %}
  Premium collection page with animated scroll reveals and modern product cards.
{% endcomment %}

<div class="collection-page">
  <div class="collection-hero reveal-text">
    <span class="collection-eyebrow">{{ collection.products_count }} Products</span>
    <h1 class="collection-title">{{ collection.title }}</h1>
    {% if collection.description != blank %}
      <p class="collection-description">{{ collection.description }}</p>
    {% endif %}
  </div>

  <div class="collection-grid-wrapper">
    {% if collection.products.size > 0 %}
      {% paginate collection.products by 12 %}
        <div class="collection-grid">
          {% for product in collection.products %}
            <div class="col-product-card reveal-card" style="--card-delay: {{ forloop.index0 | times: 80 }}ms">
              <a href="{{ product.url }}" class="col-product-card__image-link">
                <div class="col-product-card__media">
                  {% if product.featured_image %}
                    {{ product.featured_image | image_url: width: 600 | image_tag: loading: 'lazy', class: 'col-product-card__img' }}
                  {% else %}
                    {{ 'product-1' | placeholder_svg_tag: 'col-product-card__img placeholder' }}
                  {% endif %}
                  <div class="col-product-card__overlay">
                    <span class="quick-view-badge">View Product</span>
                  </div>
                </div>
              </a>
              <div class="col-product-card__body">
                {% if product.vendor != blank %}
                  <span class="col-product-card__vendor">{{ product.vendor }}</span>
                {% endif %}
                <h3 class="col-product-card__title">
                  <a href="{{ product.url }}">{{ product.title }}</a>
                </h3>
                <div class="col-product-card__footer">
                  <div class="col-product-card__pricing">
                    {% if product.compare_at_price > product.price %}
                      <span class="price-was">{{ product.compare_at_price | money }}</span>
                    {% endif %}
                    <span class="price-now">{{ product.price | money }}</span>
                  </div>
                  {% form 'product', product %}
                    <input type="hidden" name="id" value="{{ product.variants.first.id }}" />
                    <button type="submit" class="col-add-to-cart" {% unless product.available %}disabled{% endunless %}>
                      {% if product.available %}
                        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M6 2L3 6v14a2 2 0 002 2h14a2 2 0 002-2V6l-3-4z"/><line x1="3" y1="6" x2="21" y2="6"/><path d="M16 10a4 4 0 01-8 0"/></svg>
                        Add
                      {% else %}
                        Sold Out
                      {% endif %}
                    </button>
                  {% endform %}
                </div>
              </div>
            </div>
          {% endfor %}
        </div>

        {% if paginate.pages > 1 %}
          <div class="collection-pagination reveal-text">
            {{ paginate | default_pagination }}
          </div>
        {% endif %}
      {% endpaginate %}
    {% else %}
      <div class="collection-empty-state reveal-text">
        <h2 class="empty-state-title">Great things are on the horizon</h2>
        <p class="empty-state-subtitle">Something big is brewing! Our store is in the works and will be launching soon!</p>
        <a href="{{ routes.root_url }}" class="empty-state-btn">Continue Shopping</a>
      </div>
    {% endif %}
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const cards = document.querySelectorAll('.reveal-card');
    const texts = document.querySelectorAll('.reveal-text');
    const io = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          const delay = entry.target.style.getPropertyValue('--card-delay') || '0ms';
          setTimeout(() => entry.target.classList.add('is-revealed'), parseInt(delay));
          io.unobserve(entry.target);
        }
      });
    }, { threshold: 0.05 });

    [...cards, ...texts].forEach(el => io.observe(el));
  });
</script>

{% stylesheet %}
  .collection-page {
    padding-bottom: 6rem;
  }

  /* ─── Hero banner ── */
  .collection-hero {
    text-align: center;
    padding: 6rem 5% 4rem;
    background: linear-gradient(160deg, #f0faf7 0%, #fff 100%);
    border-bottom: 1px solid #eee;
    margin-bottom: 4rem;
  }
  .collection-eyebrow {
    display: inline-block;
    font-size: 0.78rem;
    font-weight: 800;
    letter-spacing: 3px;
    color: #f47f20;
    text-transform: uppercase;
    margin-bottom: 1rem;
  }
  .collection-title {
    font-size: clamp(2.5rem, 5vw, 4rem);
    font-weight: 900;
    color: #1a4f44;
    letter-spacing: -2px;
    margin: 0 0 1rem;
  }
  .collection-description {
    max-width: 600px;
    margin: 0 auto;
    color: #666;
    font-size: 1.1rem;
    line-height: 1.7;
  }

  /* ─── Grid ── */
  .collection-grid-wrapper {
    padding: 0 5%;
  }
  .collection-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
    gap: 2.5rem;
  }

  /* ─── Reveal ── */
  .reveal-text {
    opacity: 0;
    transform: translateY(30px);
    transition: opacity 0.8s cubic-bezier(0.16, 1, 0.3, 1), transform 0.8s cubic-bezier(0.16, 1, 0.3, 1);
  }
  .reveal-card {
    opacity: 0;
    transform: translateY(40px);
    transition: opacity 0.7s cubic-bezier(0.16, 1, 0.3, 1), transform 0.7s cubic-bezier(0.16, 1, 0.3, 1);
  }
  .reveal-text.is-revealed,
  .reveal-card.is-revealed {
    opacity: 1;
    transform: none;
  }

  /* ─── Product Card ── */
  .col-product-card {
    background: #fff;
    border-radius: 20px;
    overflow: hidden;
    border: 1px solid #f0f0f0;
    transition: transform 0.35s cubic-bezier(0.16, 1, 0.3, 1), box-shadow 0.35s ease;
    display: flex;
    flex-direction: column;
  }
  .col-product-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 24px 60px rgba(0,0,0,0.09);
  }
  .col-product-card__image-link {
    display: block;
    text-decoration: none;
  }
  .col-product-card__media {
    position: relative;
    aspect-ratio: 1 / 1;
    overflow: hidden;
    background: #f8f8f8;
  }
  .col-product-card__img {
    width: 100%;
    height: 100%;
    object-fit: contain;
    mix-blend-mode: multiply;
    transition: transform 0.6s cubic-bezier(0.16, 1, 0.3, 1);
  }
  .col-product-card:hover .col-product-card__img {
    transform: scale(1.06);
  }
  .col-product-card__overlay {
    position: absolute;
    inset: 0;
    background: rgba(26,79,68,0.5);
    display: flex;
    align-items: center;
    justify-content: center;
    opacity: 0;
    transition: opacity 0.3s ease;
    backdrop-filter: blur(2px);
  }
  .col-product-card:hover .col-product-card__overlay {
    opacity: 1;
  }
  .quick-view-badge {
    color: #fff;
    font-weight: 800;
    font-size: 0.9rem;
    letter-spacing: 1px;
    text-transform: uppercase;
    padding: 10px 24px;
    border: 2px solid rgba(255,255,255,0.6);
    border-radius: 100px;
    transition: background 0.3s;
  }
  .quick-view-badge:hover {
    background: rgba(255,255,255,0.15);
  }
  .col-product-card__body {
    padding: 1.5rem;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    flex: 1;
  }
  .col-product-card__vendor {
    font-size: 0.75rem;
    font-weight: 700;
    color: #f47f20;
    text-transform: uppercase;
    letter-spacing: 1px;
  }
  .col-product-card__title {
    margin: 0;
    font-size: 1.05rem;
    font-weight: 700;
  }
  .col-product-card__title a {
    text-decoration: none;
    color: #222;
    transition: color 0.2s;
  }
  .col-product-card__title a:hover {
    color: #1a4f44;
  }
  .col-product-card__footer {
    margin-top: auto;
    padding-top: 1rem;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 0.75rem;
  }
  .col-product-card__pricing {
    display: flex;
    flex-direction: column;
  }
  .price-was {
    font-size: 0.8rem;
    color: #bbb;
    text-decoration: line-through;
  }
  .price-now {
    font-size: 1.2rem;
    font-weight: 900;
    color: #1a4f44;
  }
  .col-add-to-cart {
    display: flex;
    align-items: center;
    gap: 6px;
    background: #1a4f44;
    color: #fff;
    border: none;
    padding: 10px 18px;
    border-radius: 100px;
    font-weight: 800;
    font-size: 0.85rem;
    cursor: pointer;
    transition: background 0.3s, transform 0.2s;
    white-space: nowrap;
  }
  .col-add-to-cart:hover {
    background: #f47f20;
    transform: scale(1.04);
  }
  .col-add-to-cart:disabled {
    background: #ddd;
    cursor: not-allowed;
  }

  /* ─── Pagination ── */
  .collection-pagination {
    margin-top: 4rem;
    text-align: center;
  }
  .collection-pagination .pagination {
    display: flex;
    justify-content: center;
    gap: 0.5rem;
  }
  .collection-pagination a,
  .collection-pagination span {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 44px;
    height: 44px;
    border-radius: 50%;
    font-weight: 700;
    text-decoration: none;
    color: #1a4f44;
    border: 2px solid #eee;
    transition: all 0.3s;
  }
  .collection-pagination a:hover {
    background: #1a4f44;
    color: #fff;
    border-color: #1a4f44;
  }

  @media screen and (max-width: 600px) {
    .collection-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 1rem;
    }
  }

  /* ─── Empty State ── */
  .collection-empty-state {
    text-align: center;
    padding: 5rem 2rem;
    max-width: 800px;
    margin: 0 auto;
    background: #fcfcfc;
    border-radius: 24px;
    border: 1px dashed #e0e0e0;
  }
  .empty-state-title {
    font-family: "Times New Roman", Times, serif;
    font-size: clamp(2.5rem, 5vw, 4rem);
    font-weight: 400;
    color: #1a4f44;
    margin: 0 0 1.5rem;
    line-height: 1.1;
    letter-spacing: 1px;
    text-transform: uppercase;
  }
  .empty-state-subtitle {
    font-size: 1.1rem;
    color: #555;
    margin: 0 0 3rem;
    line-height: 1.6;
  }
  .empty-state-btn {
    display: inline-block;
    background: #1a4f44;
    color: #fff;
    font-size: 0.95rem;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 1px;
    padding: 16px 36px;
    border-radius: 100px;
    text-decoration: none;
    transition: transform 0.3s ease, background 0.3s ease;
  }
  .empty-state-btn:hover {
    background: #f47f20;
    transform: translateY(-3px);
  }
{% endstylesheet %}

{% schema %}
{
  "name": "t:general.collection",
  "settings": []
}
{% endschema %}
